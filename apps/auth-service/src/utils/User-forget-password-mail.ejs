<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Your Password - CentriCommerce</title>
    <style>
      body {
        font-family: 'Segoe UI', sans-serif;
        background: #f4f4f7;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 600px;
        margin: 40px auto;
        background: #fff;
        border-radius: 18px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.05);
        overflow: hidden;
      }
      .header {
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
        padding: 40px 20px;
        text-align: center;
        color: #fff;
      }
      .header h1 {
        margin: 0;
        font-size: 28px;
        font-weight: 700;
      }
      .content {
        padding: 40px 30px;
        text-align: center;
      }
      .content h2 {
        font-size: 22px;
        color: #333;
        margin-bottom: 15px;
      }
      .content p {
        font-size: 16px;
        color: #555;
        line-height: 1.6;
        margin: 0 0 20px;
      }
      .otp-container {
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        border-radius: 12px;
        padding: 18px 22px;
        color: #fff;
        display: inline-block;
        margin: 16px 0 8px;
      }
      .otp-label {
        font-size: 13px;
        opacity: 0.9;
        letter-spacing: 0.5px;
        text-transform: uppercase;
      }
      .otp-code {
        font-size: 32px;
        letter-spacing: 8px;
        font-weight: 800;
      }
      .warning {
        background: #fff5f5;
        color: #a94442;
        border: 1px solid #f5c6cb;
        border-radius: 10px;
        padding: 12px 16px;
        margin-top: 16px;
        font-size: 14px;
        text-align: left;
      }
      .divider {
        height: 1px;
        background: #eee;
        margin: 28px 0 0;
      }
      .footer {
        padding: 24px 30px 34px;
        text-align: center;
        color: #888;
        font-size: 13px;
      }
      .brand {
        font-weight: 700;
        color: #2a5298;
        margin: 5px 0 8px;
      }
      .footer a {
        color: #007bff;
        text-decoration: none;
      }
      .footer a:hover {
        text-decoration: underline;
      }
      @media (max-width: 600px) {
        .container {
          margin: 20px;
        }
        .otp-code {
          font-size: 28px;
          letter-spacing: 6px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Password Reset Request</h1>
      </div>

      <div class="content">
        <h2>Hi <strong><%= name %></strong>,</h2>
        <p>
          We received a request to reset your CentriCommerce account password. Use the code below to verify your request and proceed with setting a new password.
        </p>

        <div class="otp-container">
          <div class="otp-label">Your Reset Code</div>
          <div class="otp-code"><%= otp %></div>
        </div>

        <div class="warning">
          <strong>Important:</strong> This code expires in <strong>5 minutes</strong> and can only be used once. If you didn't request a password reset, please ignore this email—your password won't change.
        </div>
      </div>

      <div class="divider"></div>

      <div class="footer">
        <div class="brand">CentriCommerce</div>
        <p>&copy; 2025 CentriCommerce. All rights reserved.</p>
        <p>
          Need help?
          <a href="mailto:support@centricommerce.com">Contact Support</a>
        </p>
      </div>
    </div>
  </body>
</html>
